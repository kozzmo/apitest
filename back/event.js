const events = require('events');

class MyObject extends events {

}

// const instance = new MyObject();
// instance.emit('hello');

// instance.on('hello', () => console.log('hello called'));

// Observable.fromEvent(instance, 'hello').subscribe();